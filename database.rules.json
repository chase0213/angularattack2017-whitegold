{
  "rules": {
    ".read": "true",
    ".write": "false",
    "room": {
      "$room": {
        ".write": "!data.child($room).exists() || data.child($room + '/admin').val() === auth.uid"
      }
    }
  }
}
